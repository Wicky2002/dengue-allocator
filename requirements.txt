# Streamlit Community Cloud dependency file.
#
# Community Cloud's supported dependency files are, in priority order:
# uv.lock, Pipfile, environment.yml, requirements.txt, pyproject.toml -- and
# critically, its pyproject.toml support is Poetry-specific ([tool.poetry.
# dependencies]), which this project's PEP 621 + hatchling pyproject.toml
# does not use. requirements.txt is therefore the reliable path for a cloud
# deploy, even though local development uses `pip install -e ".[dev]"`.
#
# Mirrors pyproject.toml's [project.dependencies] exactly -- kept in sync by
# hand, since Community Cloud can't read the source of truth directly. No
# `-e .` needed: app/streamlit_app.py adds src/ to sys.path itself, so
# `dengue` is importable without an editable install.
#
# Full pipeline dependencies, not just chart-rendering ones: the app's
# cold-start bootstrap (see streamlit_app.py) can run the entire Stage 1-3
# pipeline on first load, not only render cached artifacts.

numpy==1.26.4
pandas==2.2.2
pyarrow==16.1.0
scipy>=1.11,<2
scikit-learn==1.5.0
lightgbm==4.3.0
statsmodels==0.14.2
httpx==0.27.0
pdfplumber==0.11.0
beautifulsoup4==4.12.3
networkx==3.3
pulp==2.8.0
streamlit==1.61.1
altair==5.3.0
python-dotenv==1.0.1
tabulate==0.9.0
